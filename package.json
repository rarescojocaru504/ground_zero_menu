{
  "name": "ground-zero-menu",
  "private": true,
  "description": "QR menu for Ground Zero. Static site; Node is only needed for the local server and the tests.",
  "scripts": {
    "start": "http-server . -p 4173 -c-1",
    "build": "node scripts/build-deploy.js",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "http-server": "^14.1.1"
  }
}
